import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sena.entrenadorIoT',
  appName: 'entrenadorIoT',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
